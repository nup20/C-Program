#include <stdio.h>
#include <stdlib.h>

int main()
{char a[50]=("Welcome,w3resource");
    printf("Input the string:");
    gets(a);
    printf("The String you Entered is:%s",a);
    return 0;
}
