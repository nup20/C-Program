#include <stdio.h>
#include <stdlib.h>

int main()
{char a[50];
int len;
printf("Input the String=");
    gets(a);
len=strlen(a);
printf("The Length of the Input string is=%d",len);


    return 0;
}
